CREATE TABLE "JAVADB"."EMI_CALCULATION" 
   (	"CID" VARCHAR2(13 BYTE) NOT NULL ENABLE, 
	"CNAME" VARCHAR2(25 BYTE), 
	"CADD" VARCHAR2(40 BYTE), 
	"PAMT" FLOAT(126), 
	"ROI" FLOAT(126), 
	"YEARS" FLOAT(126), 
	"EMI" FLOAT(126), 
	"RESULT" FLOAT(126), 
	 CONSTRAINT "CUSTOMER_TABLE_PK" PRIMARY KEY ("CID"));